<div class="jumbotron jumbotron-fluid">
    <div class="container">
        <h1 class="display-4">Eat da Burger!</h1>
    </div>
</div>
<div class="container">
    <div class="row">
        <div class="col-sm" id="burgers-list">

            <h3>Burger List</h3>
            
                 <ul>
                {{#each burgers}}
                {{#unless devoured}}
                {{> burgers/burger-block}}
                {{/unless}}
                {{/each}}
               
            </ul>
        </div>

        <div class="col-sm"  class="form-group">
            <h3>Add a sandwhich</h3>
            <form id="add-burger" method= "POST" action="/api/burger" class="create-form">
                <div class="input-group input-group-sm mb-3"  >
                    <div class="input-group-prepend">
                    </div>
                    <input type="text" id="burger-input" name="burger_name" class="form-control"
                        aria-label="Sizing example input" aria-describedby="inputGroup-sizing-sm">
                </div> 
                <button type="submit" class="btn btn-primary" id="submit-button">Submit</button>
            </form>
        </div>

        <div class="col-sm">
            <div id="devoured-list">
                <ul>
                    {{#each burgers}}
                    {{#if devoured}}
                    {{> burgers/burger-block2}}
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>

    </div>
</div>

